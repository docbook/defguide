defaultTasks 'tdg5'

task tdg5(type: GradleBuild) {
  buildFile = "en/defguide5/build.gradle"
  tasks << 'tdg'
}

task buildclean(type: GradleBuild) {
  buildFile = "en/defguide5/build.gradle"
  tasks << 'clean'
}
